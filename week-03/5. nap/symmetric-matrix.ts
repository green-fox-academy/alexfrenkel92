'use strict';
//Create a function named isSymmetric that takes an n×n integer matrix (two dimensional array/list) as a parameter and returns true if the matrix is symmetric or false if it is not.

let arr1: number[] = [1, 2, 3];
let arr2: number[] = [2, 11, 4];
let arr3: number[] = [3, 4, 5];

// function isSymmetric (n) {
// }

    if (arr1[1] == arr2[0] && arr1[2] == arr3[0] && arr3[1] == arr2[2])


// for (let i: number = 0; i < arr1.length; i++) 
// for (let j: number = 0; j < arr2.length; j++)
// for (let k: number = 0; k < arr3.length; k++)